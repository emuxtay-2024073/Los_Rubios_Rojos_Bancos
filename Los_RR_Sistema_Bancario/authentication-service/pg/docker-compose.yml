version: '3.8'
services:
  db:
    image: postgres:13
    container_name: bancario-auth-db
    environment:
      POSTGRES_USER: RUBIOSR
      POSTGRES_PASSWORD: Bancos123!
      POSTGRES_DB: bancos_db
    ports:
      - "5436:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: